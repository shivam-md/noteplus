// -- this model property is used to map record from the user credential in the login and registration form.

class LoginUser {
  // properties of login credentials used.
  final String? email;
  final String? password;

  LoginUser(this.email, this.password);
}